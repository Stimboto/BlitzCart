<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/activity_home.xml -->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:background="#CDC4EC"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <!-- Main content layout -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Toolbar -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="#E2DAFB"
            android:elevation="4dp"
            app:menu="@menu/menu_home"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light">
        </androidx.appcompat.widget.Toolbar>


        <!-- NestedScrollView for scrollable content -->
        <androidx.core.widget.NestedScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/toolbar"
            android:fillViewport="true">

            <!-- LinearLayout to contain multiple scrollable views -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <!-- Content 1 -->
                <FrameLayout
                    android:id="@+id/fragmentContainer"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content" />

                <!-- video adv -->
                <VideoView
                    android:id="@+id/videoViewAd"
                    android:layout_width="match_parent"
                    android:layout_height="200dp"
                    android:layout_below="@+id/toolbar"
                    android:layout_marginTop="16dp"/>


                <!-- Add this below your toolbar in activity_home.xml -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerViewAds3"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_below="@id/toolbar"
                    android:layout_marginTop="8dp"
                    android:scrollbars="horizontal"
                    android:nestedScrollingEnabled="false"/>


                <!-- RecyclerView for brand posters -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/brandRecyclerView"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:scrollbars="vertical"
                    android:nestedScrollingEnabled="false"
                    android:layout_marginTop="8dp" />

                <!-- RecyclerView for Categories -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/categoryRecycler"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_below="@id/brandRecyclerView"
                    android:padding="16dp"
                    android:clipToPadding="false"
                    app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
                    android:orientation="horizontal"/>

                <!-- RecyclerView for ADs -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerViewAds"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:layout_below="@id/categoryRecycler"
                    android:scrollbars="horizontal"
                    android:layout_marginTop="8dp" />

                <!-- RecyclerView for products -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerViewProducts"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_below="@+id/recyclerViewAds"
                    android:layout_marginTop="16dp"
                    android:layout_marginBottom="8dp"/>

                <!-- RecyclerView for the ADV2 section -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerViewAds2"
                    android:layout_width="match_parent"
                    android:layout_height="160dp"
                    android:layout_below="@id/recyclerViewKitchen"
                    android:layout_marginTop="16dp"
                    android:layout_marginBottom="8dp"/>


                <!-- RecyclerView for kitchen products -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerViewKitchen"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_below="@id/recyclerViewProducts"
                    android:layout_above="@id/bottomNavigationView"
                    android:layout_marginTop="16dp"
                    android:layout_marginBottom="16dp"/>

                <!-- Add more content views as needed -->

            </LinearLayout>

        </androidx.core.widget.NestedScrollView>

        <!-- Bottom Navigation Bar -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavigationView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_alignParentBottom="true"
            android:background="#E2DAFB"
            app:itemIconTint="@color/bottom_nav_icon_color"
            app:itemTextColor="@color/bottom_nav_text_color"
            app:menu="@menu/bottom_nav_menu" />
    </RelativeLayout>

    <!-- NavigationView for side page -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#79CAE3"
        app:headerLayout="@layout/nav_header_home"
        app:menu="@menu/nav_menu_home" >
    </com.google.android.material.navigation.NavigationView>


</androidx.drawerlayout.widget.DrawerLayout>
